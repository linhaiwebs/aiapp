# ============================================================
# XCAI 端云智采 - Docker 部署环境配置
# 复制此文件为 .env 后根据实际情况修改
# cp .env.example .env
# ============================================================

# ===== 端口配置（支持自定义端口）=====
# ┌──────────────────────────────────────────────────────┐
# │ 如果服务器已有 Nginx（宝塔等），推荐使用：              │
# │   docker-compose.host-nginx.yml                     │
# │ 只需配置 SERVER_PORT，由宿主机 Nginx 反向代理         │
# │                                                      │
# │ 如果服务器没有 Nginx，使用：                           │
# │   docker-compose.yml（含 Docker 内 Nginx）            │
# │ 需要配置 NGINX_HTTP_PORT / NGINX_HTTPS_PORT          │
# └──────────────────────────────────────────────────────┘

# 宿主机 Nginx 模式：只需配置此端口
# 后端 API 端口，宿主机 Nginx 反向代理到此端口
SERVER_PORT=3000

# Docker 内 Nginx 模式：配置对外端口（host-nginx 模式可忽略）
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# 以下端口一般不需要暴露到宿主机（留空则不映射）
POSTGRES_PORT=
REDIS_PORT=
MINIO_API_PORT=
MINIO_CONSOLE_PORT=

# ===== 数据库配置 =====
DB_TYPE=postgres
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=xcai
# ⚠️ 首次部署必须为 true，让 TypeORM 自动创建数据库表
# 表创建完成后可改为 false 提升性能
DB_SYNCHRONIZE=true
DB_LOGGING=false

# ===== Redis 配置 =====
REDIS_ENABLED=true
REDIS_PASSWORD=

# ===== 存储配置 =====
# 可选：minio / local / oss
STORAGE_TYPE=minio

# MinIO 配置（STORAGE_TYPE=minio 时使用）
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=xcai
MINIO_USE_SSL=false

# 阿里云 OSS 配置（STORAGE_TYPE=oss 时使用）
# 申请流程参见 DEPLOY_GUIDE.md
OSS_REGION=oss-cn-hangzhou
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=
OSS_BUCKET=xcai
OSS_ENDPOINT=
# CDN 加速域名（推荐配置，用于文件下载/播放，留空则用预签名 URL）
OSS_CDN_DOMAIN=

# ===== JWT 安全配置 =====
# ⚠️ 生产环境务必修改为随机长字符串！
# 可用: openssl rand -base64 48
JWT_SECRET=change-this-in-production
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ===== CORS 跨域配置 =====
# 允许的前端域名，多个用逗号分隔
CORS_ORIGINS=https://blackend.duanfukeji.com,https://admin.blackend.duanfukeji.com

# ===== 短信配置 =====
# mock = 不发真短信（开发/测试用，验证码打印到控制台）
# market = 阿里云市场短信服务
SMS_PROVIDER=market
SMS_APP_CODE=f2b07360c1a04805a5b3cc9367b45141
SMS_TEMPLATE_ID=lxym_20111_sdgsfhwqgvyh
SMS_MARKET_HOST=send.market.alicloudapi.com
SMS_MARKET_PATH=/sms/send
SMS_CODE_LENGTH=6
SMS_CODE_TTL=300
SMS_CODE_COOLDOWN=60
SMS_MAX_SEND_PER_DAY=10

# ===== 实名认证配置 =====
# APPCODE 留空则复用 SMS_APP_CODE
REAL_NAME_APP_CODE=
REAL_NAME_VERIFY_HOST=zidv2.market.alicloudapi.com
REAL_NAME_VERIFY_PATH=/idcheck/Post
REAL_NAME_OCR_HOST=zidv2.market.alicloudapi.com
REAL_NAME_OCR_FRONT_PATH=/thirdnode/ImageAI/idcardfrontrecongnition
REAL_NAME_OCR_BACK_PATH=/thirdnode/ImageAI/idcardbackrecongnition

# ===== APP 更新控制 =====
APP_VERSION=1.0.0
APP_VERSION_CODE=1
APP_DOWNLOAD_URL=https://blackend.duanfukeji.com/dist/app-release.apk
APP_CHANGELOG=初始版本
APP_FORCE_UPDATE=false
